spring:
  profiles:
    group:
      stage: stageDB, jasypt, oauth2, jwt, s3
      prod:
      test:
    active: stage
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 30MB
      max-request-size: 10MB

# JPA Log
logging:
  level:
    org:
      hibernate:
        sql: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
